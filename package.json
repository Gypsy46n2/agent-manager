{
  "name": "agent-manager",
  "version": "0.1.0",
  "description": "Unattended Plan -> Draft -> Review -> Apply pipeline for delegating coding/documentation tasks to a local LLM, with a deterministic (no-LLM) apply step and a plugin registry for task sources.",
  "license": "MIT",
  "main": "src/task-source-registry.js",
  "files": [
    "src",
    "ps1",
    "LICENSE",
    "README.md"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  },
  "scripts": {
    "test": "node --test src/*.test.js src/**/*.test.js *.test.js",
    "drift-scan": "node src/drift-scan.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  }
}
